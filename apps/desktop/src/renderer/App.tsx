/**
 * Root application component.
 *
 * Placeholder component while design system is being rebuilt.
 * Components will be developed in Ladle sandbox first.
 */
export function App() {
  return (
    <div className="flex h-screen items-center justify-center bg-background">
      <div className="text-center space-y-4">
        <h1 className="text-2xl font-semibold text-foreground">TypeNote</h1>
        <p className="text-muted-foreground">Design system reset — ready for rebuild</p>
      </div>
    </div>
  );
}
