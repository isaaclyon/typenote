{
  "name": "@typenote/cli",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "bin": {
    "typenote": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@typenote/api": "workspace:*",
    "@typenote/core": "workspace:*",
    "@typenote/storage": "workspace:*",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
