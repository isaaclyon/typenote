/**
 * Default values used across components.
 */

/**
 * Default icon color (warm gray) used when no type-specific color is provided.
 * Matches --color-gray-500 from tokens.
 */
export const DEFAULT_ICON_COLOR = '#78716c';
