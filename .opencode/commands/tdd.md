---
description: Start a TDD cycle for feature or bugfix
argument-hint: <optional additional instructions or context>
agent: build
---

Use the `test-driven-development` skill and follow it exactly as presented.

If the user provided additional instructions or context, incorporate them:
$ARGUMENTS
