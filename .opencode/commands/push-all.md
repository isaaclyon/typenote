---
description: Push current branch to remote
agent: build
---

Push the current branch to the remote.

!`git push`

If the push fails due to no upstream, set the upstream and retry using the current branch name.
